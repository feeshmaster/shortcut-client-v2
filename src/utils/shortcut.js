export default class Shortcut {
    constructor(name) {
      this.name = name
    }
    
    build() {
      //creates template

      let template = `
      <div class="__shortcut__"></div>`
    }
}
